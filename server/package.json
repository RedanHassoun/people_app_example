{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc logger.ts && tsc repository.ts && tsc data.service.ts && tsc server.ts && node server.js",
    "node-server": "tsc logger.ts && tsc repository.ts && tsc data.service.ts && tsc server.ts && node server.js"
  },
  "author": "redan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "mongodb": "^3.1.13",
    "mongoose": "^5.4.11",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^10.12.24"
  }
}
